# 📦 Сервис загрузки изображений

Простое веб-приложение на FastAPI с фронтендом на HTML + JS и проксированием через Nginx. Позволяет загружать изображения через drag-and-drop интерфейс и получать прямые ссылки на файлы.

## 🛠️ Требования к окружению

- **Docker** ≥ 20.10
- **Docker Compose** ≥ 1.29
- (опционально) Python 3.13 — только если вы хотите запускать сервер вручную

## 📂 Структура проекта
```
├── db_init/              # Инициализация базы данных
├── images/               # Загруженные изображения (volume)
├── logs/                 # Логи сервиса (volume)
├── static/               # Статические ресурсы (CSS/JS)
├── templates             # Каталог шаблонов
├── .env                  # Переменные окружения
├── app.py                # Основной файл FastAPI-бэкенда
├── backup.sh             # Сохранение резервной копии базы данных
├── docker-compose.yml    # Компоновка сервисов backend + nginx
├── Dockerfile            # Инструкция сборки образа FastAPI
├── index.html            # Клиентский интерфейс (drag-and-drop загрузка)
├── nginx.conf            # Конфигурация прокси-сервера Nginx
├── requirements.txt      # Зависимости Python
├── wait-for-it.sh        # Скрипт проверяет доступность TCP порта

```

## 🚀 Развертывание

1. Клонируйте проект и перейдите в директорию:
```bash
    git clone <URL проекта>
    cd <папка проекта>
```

2. Создать файл с переменными окружения `.env` в корне проекта
```bash
POSTGRES_USER=myuser
POSTGRES_PASSWORD=mypassword
POSTGRES_DB=mydb
POSTGRES_HOST=db
POSTGRES_CONTAINER=server_images_postgres
```

3. Соберите и запустите контейнеры: 
```bash
    docker compose up --build
```

3. Откройте в браузере:
    http://localhost:8080

## 🛑 Остановка

Для остановки контейнеров:
```bash
  docker compose down
```


## 🌐 Маршруты и API

**POST /upload**
- Назначение: Загрузка изображений
- Тип запроса: multipart/form-data
- Поле: file
- Ограничения:
  - Макс. размер: 5 МБ
  - Поддерживаемые форматы: .jpg, .jpeg, .png, .gif
- Ответ:
  {
    "url": "/images/<уникальное_имя>"
  }

**GET /images/<имя>**
- Статический доступ к ранее загруженным изображениям

**GET / (главная)**
- Интерфейс загрузки изображений (drag-and-drop)

**GET /images-list**
- Список загруженных изображений
- Параметры
  - page=(int) - номер страницы (по 10 изображений)
  - order=(ASC|DESC) - сортировка по дате загрузки
- Возможности: просмотр, скачивание, удаление
- Примеры:
  - `/images-list?page=N`
  - `/images-list?order=ASC`
  - `/images-list?page=N&order=ASC`

**GET /delete/id**
- удаляет запись об изображении из базы данных
- удаляет физический файл из папки `/images`
- Ответ: Location: /images-list

## ⚙️ Сервисы в docker-compose
```text
services:
  app:                                           # FastAPI-сервер
    build: .
    volumes:
      - ./app.py:/app/app.py
      - ./logs:/app/logs
      - ./images:/app/images
      - ./templates:/app/templates
    container_name: server_images_backend
    expose:
      - 8000
    depends_on:
      - db
    networks:
      app-net:

  nginx:
    image: nginx:stable                         # Фронтенд + прокси
    container_name: server_images_frontend
    ports:
    - "8080:80"
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
      - ./index.html:/var/www/public/index.html:ro
      - ./static:/var/www/public/static
      - ./logs:/var/www/logs
      - ./images:/var/www/public/images
    depends_on:
      - app
    networks:
      app-net:

  db:                                           # PostgreSQL база данных
    container_name: ${POSTGRES_CONTAINER}
    image: postgres:17
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - pgdata:/var/lib/postgresql/data
      - ./db_init:/docker-entrypoint-initdb.d
    networks:
      app-net:
    ports:
      - 5433:5432

networks:
  app-net:

volumes:
  pgdata:


```

## 🔍 Валидация и логирование

- Файлы проходят:
  - Проверку расширения
  - Проверку размера
  - Проверку валидности изображения (через Pillow)
- Все действия логируются в logs/app.log
- Логи Nginx: logs/access.log, logs/error.log

## 🖼️ Веб-интерфейс

- Drag-and-drop или выбор файла
- Превью изображения + ссылка
- Кнопка COPY для копирования URL
- Страница списка изображений (пагинация + сортировка по дате загрузки кликабельно)

## Резервное копирование данных
- Для создания резервной копии
- Убедиться, скрипт имеет права на выполнение `chmod +x backup.sh`
- Выполнить `./backup.sh`, будет создан каталог `backups`
- Контейнер должен быть запущен
- Результат выполнения:
  - Резервная копия успешно создана: `./backups/backup_2025-09-14_173431.sql`

## Восстановление из резервной копии
- в каталоге проекта выполнить ls backups
- выбрать доступную резервную копию
- выполнить команду `./backup.sh backup_2025-09-14_173431.sql`
  - Продолжить? (y/n): y (подтвердить, ***база данных будет очищена***)
  - +++ База данных успешно восстановлена из ./backups/backup_2025-09-14_173431.sql

## Создание резервной копии по расписанию
- используется crontab
- в каталоге проекта выполнить `pwd` скопровать путь
- выполнить `crontab -e` откроется редактор 
- добавить строку (пример - запуск каждый день в 03:00)
- 0 3 * * * cd /ПУТЬ/К/ПРОЕКТУ && ./backup.sh
- проверить `crontab -l`

## 📦 Зависимости (requirements.txt)

```txt
fastapi
uvicorn
aiofiles
pillow
python-multipart

Полный список см. в requirements.txt.
```


✅ Проверка

1. Загрузите изображение через интерфейс.
2. Проверьте:
   - Ссылка работает
   - Изображение отображается
   - В logs/app.log появилась запись о загрузке
